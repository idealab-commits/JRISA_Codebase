Write a Cursor prompt for the JRISA codebase. The agent must not change model
architecture, loss equations, or Algorithms 1–3. It may only fix the
data-augmentation shape bug described below, then rerun the existing pilot.

Facts:
- Trainable data is CREMI only: data/cremi/sample_{A,B,C}_raw.npy and
  sample_{A,B,C}_labels.npy.
- Each volume is (125, 1250, 1250). Labels are neuron instance ids, remapped
  to 1..K, with no background voxel. Counts: A 37366, B 1309, C 1978.
- AC3, AC4, and MitoEM are not usable. Do not download or invent them.
- Pilot command that already reached the GPU, then crashed before epoch 0:
  python src/train.py --data-dir ./data/cremi --dataset cremi --config-name full --seed 0 --out-dir ./runs/pilot --max-epochs 10 --iterations-per-epoch 20 --early-stop-patience 100
- Crash: RuntimeError: stack expects each tensor to be equal size, but got
  [1, 16, 320, 384] at entry 0 and [1, 16, 384, 320] at entry 1.
- Cause: src/data.py _augment applies np.rot90 on lateral axes of a
  non-square 384x320 patch; a 90 or 270 degree rotation swaps H and W,
  producing a batch with mixed shapes.

Chosen fix: in the _augment staticmethod in src/data.py, restrict the
rotation step so it is SHAPE-AWARE:
  - If the patch's lateral dimensions (H, W) are equal (square, e.g. the
    16x320x320 AC3/AC4/MitoEM patch shape), keep the existing behavior:
    k = np.random.randint(0, 4)  # allows 0, 90, 180, 270 degrees
  - If H != W (non-square, e.g. CREMI's 16x384x320), restrict rotation to
    shape-preserving angles only:
    k = np.random.choice([0, 2])  # 0 or 180 degrees only, via np.rot90(..., k)
  Keep both flip operations (axis=1, axis=2) unchanged in both cases -- flips
  never change H/W shape, so they are not part of the bug and do not need
  restriction.
  Do not touch anything else in data.py, and do not touch model.py,
  losses.py, decode.py, metrics.py, train.py, infer.py, or evaluate.py.

- After the fix, rerun that same pilot command exactly as given above. Do
  not start the 600-epoch runs.
- Report the full traceback if it fails again. If it finishes, show
  runs/pilot/train_log.jsonl and tell me whether train_loss decreased across
  the 10 logged epochs.
- Checkpoints: last.pt is written each epoch. best.pt is written only when
  --val-data-dir is set. This pilot has no val dir, so only last.pt is
  expected -- confirm it exists in runs/pilot/ after the run.
- Show me the exact diff of the change to src/data.py before considering
  this task complete.
